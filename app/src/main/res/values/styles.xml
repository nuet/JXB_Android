<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="android:Theme.Light.NoTitleBar">
    </style>

    <style name="WebViewTheme" parent="Theme.AppCompat.Light.NoActionBar">
    </style>

    <style name="SplashTheme" parent="android:Theme.Translucent.NoTitleBar.Fullscreen">
        <!--<item name="android:windowBackground">@drawable/splash_background</item>-->
    </style>

    <style name="LaunchActivityTheme" parent="AppTheme">
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="financing_menu_text">
        <item name="android:textColor">@color/selector_financing_menu_text</item>
        <item name="android:textSize">15sp</item>
        <item name="android:paddingTop">12dp</item>
        <item name="android:paddingBottom">10dp</item>
        <item name="android:gravity">center</item>
        <item name="android:clickable">true</item>
    </style>
    <style name="topMenu" >
        <item name="tmb_padding">10dp</item>
        <item name="tmb_textSize">18sp</item>
        <item name="tmb_textColor">@android:color/white</item>
        <item name="tmb_backgroundColor">@color/blue</item>
        <item name="tmb_backSrc">@mipmap/top_menu_back</item>
        <item name="tmb_menuSrc">@mipmap/top_menu_more</item>
    </style>

    <!--popupWindow-->
    <style name="mypopwindow_anim_style">
        <!-- 指定显示的动画xml -->
        <item name="android:windowEnterAnimation">@anim/popshow_anim</item>

        <!-- 指定消失的动画xml -->
        <item name="android:windowExitAnimation">@anim/pophidden_anim</item>
    </style>

    <!-- iOSActionSheet底部Dialog的样式 ，有模糊效果 -->
    <style name="iOSActionSheetDialogStyle" parent="@android:style/Theme.Dialog">

        <!-- 背景透明 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <!-- 浮于Activity之上 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 边框 -->
        <item name="android:windowFrame">@null</item>
        <!-- Dialog以外的区域模糊效果 -->
        <item name="android:backgroundDimEnabled">true</item>
        <!-- 无标题 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 半透明 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- Dialog进入及退出动画 -->
        <item name="android:windowAnimationStyle">@style/iOSActionSheetDialogAnimation</item>
    </style>

    <!-- iOSActionSheet进出动画 -->
    <style name="iOSActionSheetDialogAnimation" parent="@android:style/Animation.Dialog">
        <item name="android:windowEnterAnimation">@anim/iosactionsheet_dialog_in</item>
        <item name="android:windowExitAnimation">@anim/iosactionsheet_dialog_out</item>
    </style>


    <!-- 自定义仿IOS的AlertDialog的样式 -->
    <style name="iOSAlertDialogStyle" parent="@android:style/Theme.Dialog">
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:backgroundDimEnabled">true</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>



</resources>
